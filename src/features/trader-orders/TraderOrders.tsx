export default function TraderOrders() {
  return (
    <div className="space-y-6">
      <h1 className="text-2xl font-bold" style={{ color: 'var(--text-primary)' }}>سفارش‌های من</h1>
      <p className="text-sm" style={{ color: 'var(--text-secondary)' }}>تب‌ها: باز — pending — تکمیل‌شده — لغوشده</p>
    </div>
  );
}
